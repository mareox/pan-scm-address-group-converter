# Strata Cloud Manager Authentication Credentials
# Copy this file to '.env' and fill in your actual credentials

# OAuth 2.0 Client Credentials (Required)
SCM_CLIENT_ID=your_client_id_here
SCM_CLIENT_SECRET=your_client_secret_here
SCM_TSG_ID=your_tenant_service_group_id_here

# Optional Configuration
# SCM_LOG_LEVEL=DEBUG

# Notes:
# 1. Never commit the actual .env file with real credentials to version control
# 2. Obtain these credentials from your Strata Cloud Manager service account
# 3. The TSG_ID identifies your tenant service group in Strata Cloud Manager
# 4. Log level can be: DEBUG, INFO, WARNING, ERROR (default: INFO)